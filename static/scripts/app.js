// const img = document.getElementById("map")
// img.onclick = (e) => {
//   const x = e.pageX - e.target.offsetLeft;
//   const y = e.pageY - e.target.offsetTop;
//   console.log(x, y);

//     document.getElementById("lat").value = x;
//     document.getElementById("long").value = y;
//   }

// still gotta get the conversion to lat and long coords, and since all files aren't in the same folder i might need to just change the route

// ^ still need conversion but script is in html script tags now.
